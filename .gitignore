# macOS
.DS_Store
.AppleDouble
.LSOverride

# Cursor / editor agent workspace — local only, never publish
.cursor/
.cursorignore
AGENTS.md

# Internal session diary (kept on disk for the maintainer, not in git)
docs/PROGRESS.md
docs/06-stage6-verification.md

# Secrets — never commit tokens, keys, or dumps
.env
.env.*
!.env.example
*.pem
*.p12
*.key
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
credentials.json
secrets/
*.keychain
keychain-dump*
*.tok

# Swift / Xcode
.build/
.swiftpm/xcode/
xcuserdata/
*.xcodeproj
*.xcworkspace
DerivedData/
*.hmap
*.ipa
*.dSYM
*.dSYM.zip

# Runtime data (daemon workspace, SQLite, logs)
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite-wal
*.sqlite-shm
incoming/
*.log
workspace/

# Accidental vault metadata
.obsidian/
